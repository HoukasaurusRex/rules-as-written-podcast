{
  "private": true,
  "workspaces": [
    "astro-site"
  ],
  "scripts": {
    "test:e2e": "yarn playwright test --config=e2e/playwright.config.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "husky": "^9.1.7"
  }
}
