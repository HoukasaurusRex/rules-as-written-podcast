name: Build, Test & Deploy

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - run: yarn install

      - run: yarn fetch-srd

      - run: yarn check

      - run: yarn netlify build
        env:
          NOTION_TOKEN_CI: ${{ secrets.NOTION_TOKEN }}
          NOTION_DB_ID_CI: ${{ secrets.NOTION_DB_ID }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            dist/
            .netlify/

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - run: yarn install

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ hashFiles('**/yarn.lock') }}

      - run: yarn playwright install --with-deps chromium

      - uses: actions/download-artifact@v4
        with:
          name: build-output

      - name: Run e2e tests
        run: yarn test:e2e:ci
        env:
          BASE_URL: http://localhost:4173

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    permissions:
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - run: yarn install

      - uses: actions/download-artifact@v4
        with:
          name: build-output

      - name: Deploy to Netlify
        run: yarn netlify deploy --no-build --prod
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Create GitHub Deployment
        uses: actions/github-script@v7
        with:
          script: |
            const script = await import('${{ github.workspace }}/.github/scripts/create-deployment.mjs')
            await script.default({ github, context })
